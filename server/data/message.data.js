const db = require('./connectdb.data').connect();

module.exports = {
    getMessages : (userID) =>{
        //Codigo para obtener los mensajes de un usuario por su id
    },
    getMessages : (number) =>{
        //Codigo para obtener los mensajes de un usuario
    }
}